"""Online drone navigation agent client.

Public runtime entry points live in :mod:`agent.run_agent`; keeping package import
lightweight also allows protocol-only users to avoid loading policy backends.
"""
