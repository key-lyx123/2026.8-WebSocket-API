# 开题报告：基于 SOTA LLM 蒸馏的无人机 3D 避障具身 Agent

## 题目：SOTA LLM 蒸馏--用大模型专家数据微调小模型实现 3D 避障导航

**Distilling SOTA LLM Expertise: Fine-Tuning Small Models for 3D Aerial Obstacle Avoidance**

> 日期：2026 年 8 月 6 日
> 团队：001–010（10 人）
> 配套接口文档：`proposal/api.md`

---

## 一、问题是什么？为什么有价值？

### 1.1 问题

无人机在 3D 障碍物环境中自主导航到目标点，需要同时做三件事：感知周围障碍物、规划安全路径、输出飞行控制指令。传统方案用经典算法（A\*、势场法、RRT\*）做规划，但它们各有局限：A\* 在连续空间分辨率受限、势场法陷局部极小、RRT\* 路径不平滑。

我们的核心问题是：**能否用大语言模型（LLM）作为无人机的决策大脑，在 3D 仿真环境中完成避障导航？** 具体而言，给定无人机的 3D 状态（位置、速度、朝向）和传感器感知（LiDAR 点云、超声波、高度），让 LLM 输出离散飞行动作（前进/后退/上升/下降等 10 种），引导无人机规避障碍到达目标。

### 1.2 为什么有价值

1. **LLM 作为具身 Agent 是当前最前沿方向**。2025–2026 年 VLA（Vision-Language-Action）、VLN（Vision-Language Navigation）方向爆发，但从零训练 VLA 需要海量视觉数据和算力。用文本状态描述 + LLM 决策是最小可行的具身 Agent 验证。

2. **SOTA LLM 是天然好的导航专家**。deeoseek-v4-flash等模型理解 3D 空间几何、能推理"前方有柱子需要绕行"或"障碍矮可以飞越"，但它们太慢（秒级/步），无法实时跑 10Hz 控制闭环。如果能把 SOTA LLM 的导航能力蒸馏到一个 7B 小模型里，就能用低成本部署。

---

## 二、前人方案及其优缺点

我们不追求穷举文献，而是枚举显而易见的解决方案，逐个分析为什么不够好。

### 方案 A：直接用 SOTA LLM 做实时控制

**做法**：sota llm 直接连仿真，每步推理输出动作。

**优点**：
- 零训练成本，即插即用
- 推理能力强，能理解复杂 3D 场景
- 天然有 CoT 推理过程

**缺点**：
- **太慢**：SOTA LLM 单步推理 1–5 秒，仿真需要 10Hz（100ms/步），差 10–50 倍
- **太贵**：每个 episode ~80 步 × 100 场景 = 8000 次 API 调用，成本高
- **不可部署**：无法在无人机机载计算上运行，依赖云端 API
- **上下文爆炸**：多轮对话累积到 80 步时上下文 30K+ tokens，推理越来越慢

### 方案 B：经典算法（A\* / 势场法 / RRT\*）

**做法**：用程序化算法做路径规划 + 控制器跟踪。

**优点**：
- 极快（毫秒级）
- 无需训练
- 可解释、可调试

**缺点**：
- **A\***：栅格化损失精度，连续空间路径不光滑，转弯生硬
- **势场法**：局部极小值问题（被障碍围住时卡死），参数难调
- **RRT\***：路径随机性大，不够平滑，实时性差
- **共同缺点**：没有"理解"能力，无法利用语义信息（如"这是一根柱子可以从旁边绕"），泛化到未见过的场景时可能失效
- **没有推理过程**：无法解释为什么选这条路，不利于后续改进和调试

### 方案 C：直接用未微调的 7B LLM（zero-shot）

**做法**：Qwen2.5-7B-Instruct 原始模型，不做任何微调，直接给状态文本让它输出动作。

**优点**：
- 比 SOTA LLM 快（7B 模型推理 ~200ms/步）
- 可本地部署（单 4090 即可）
- 零训练成本

**缺点**：
- **没学过这个任务**：7B 模型虽然理解语言，但从没见过"无人机状态 -> 动作"的映射，需要大量 in-context 示例才能勉强工作
- **输出不可靠**：可能输出非数字字符、幻觉动作、完全无视障碍物
- **无 CoT 能力**：小模型 zero-shot 的推理质量远不如 SOTA LLM

### 方案 D：强化学习（从零训练）

**做法**：用 SAC/PPO 等算法从零开始训练一个策略网络。

**优点**：
- 可以超越人类设计的启发式
- 推理极快（前向传播毫秒级）

**缺点**：
- **样本效率极低**：3D 避障的奖励稀疏（只有到达目标才有大奖励），可能需要数百万步仿真才能收敛
- **不可解释**：黑箱策略，无法知道为什么选这个动作
- **仿真器瓶颈**：Webots 单实例仿真慢，多实例并行困难
- **无语言能力**：纯数值策略，无法利用自然语言指令或推理

### 方案 E：程序专家 + SFT（不经过 SOTA LLM）

**做法**：用 A\* / 势场法生成专家轨迹，直接作为 SFT 数据训练 7B 模型。

**优点**：
- 数据生成快（程序毫秒级）
- 专家轨迹质量有保证（A\* 是最优路径）

**缺点**：
- **蒸馏天花板 = 专家水平**：如果专家本身不好（势场法陷局部极小），模型也学不好
- **无推理过程**：程序算出来的动作没有"为什么"的解释，SFT 数据只有 `(state, action)` 对，模型学到的是映射而非理解
- **泛化受限**：模型学的是 A\* 的规则，在 A\* 处理不好的场景（如需要绕大弯、需要高度变化）也会表现差

---

## 三、我们的方案是什么？好在哪里？

### 3.1 方案概述

**用 SOTA LLM 作为专家在仿真中收集带推理过程的多轮对话数据，蒸馏微调 7B 小模型。**

```
SOTA LLM (deepseek)          微调后 Qwen2.5-7B
     │                                  │
     │ 离线跑仿真 (慢但无所谓)             │ 在线推理 (快 enough)
     │ Hermes Agent 累积上下文             │ Hermes Agent 累积上下文
     │ 每步输出 reasoning + action        │ 每步输出 reasoning + action
     │                                  │
     ▼                                  ▼
SFT 数据 (多轮对话)               部署/评测
含 reasoning_content               只解析 action 字段
```

### 3.2 具体做法

**第一步：仿真环境搭建**（`src/sim/`）

Webots 3D 仿真，搭载富传感器的无人机（3D LiDAR 16 层 360°、RTK 定位 ±1cm、IMU、ToF、8 向超声波）。场景生成保证有解性（BFS 可达性检查），且 3D 有意义（柱类必须绕行 / 矮类可飞越 / 悬空类需下降），让 z 轴真正参与决策。WebSocket API 标准化仿真侧与 client 的通信（详见 `api.md`）。

**第二步：SOTA LLM 离线数据采集**（`src/data/`）

SOTA LLM（Claude / GPT-4）通过 Hermes Agent 框架连接仿真，作为 WebSocket client。每个 episode 是一个多轮对话：

- 每步：`serialize_state(obs)` 生成状态文本 -> 追加为 user 消息 -> SOTA LLM 推理输出 `reasoning_content`（CoT 推理）+ `content`（动作编号）-> 追加为 assistant 消息 -> 解析动作发送给仿真
- Hermes Agent 累积完整对话历史，第 N 步能看到前 N-1 步全部状态和动作
- 成功 episode 保留，失败丢弃

**第三步：SFT 微调**（`src/training/`）

用采集的多轮对话数据（含 `reasoning_content`）做 LoRA 微调 Qwen2.5-7B-Instruct。训练时模型学习生成推理过程 + 动作；推理时只解析动作字段。

**第四步：评测**（`src/eval/`）

微调后模型 + Hermes Agent 连接仿真跑测试场景，与基线（随机/直行/势场法/未微调 LLM/SOTA LLM）对比成功率、碰撞率、路径效率等指标。

### 3.3 好在哪里

相比前人方案，我们的方案解决了它们各自的核心缺陷：

| 对比 | 前人方案的缺陷 | 我们的解法 |
|---|---|---|
| vs 方案 A（SOTA LLM 实时） | 太慢太贵不可部署 | SOTA LLM 只在离线数据收集时用，部署的是微调后 7B 模型 |
| vs 方案 B（经典算法） | 无理解能力，泛化差，无推理过程 | LLM 能理解 3D 空间语义，reasoning_content 提供可解释推理 |
| vs 方案 C（zero-shot 7B） | 没学过任务，输出不可靠 | SFT 让 7B 模型学会"状态 -> 推理 -> 动作"的完整映射 |
| vs 方案 D（RL 从零） | 样本效率极低，黑箱 | SFT 用 SOTA LLM 专家数据，样本效率高；有推理过程可解释 |
| vs 方案 E（程序专家 + SFT） | 蒸馏天花板 = 程序专家水平；无推理过程 | SOTA LLM 是比程序更好的专家（理解语义、有推理）；reasoning_content 让模型学的是"为什么"而非"是什么" |

**核心创新点**：

1. **SOTA LLM 即专家**：不经过程序专家中转，直接让 SOTA LLM 在仿真中做决策。SOTA LLM 比 A\*/势场法更聪明--它能理解"这个障碍矮可以飞越"而不是机械地绕行，且推理过程天然带 CoT。

2. **训练/推理零 mismatch**：数据收集和推理都用 Hermes Agent + 累积上下文 + 同一个 `serialize_state` 函数。唯一区别是模型不同（SOTA vs 微调 Qwen）。训练时见到的对话格式与推理时完全一致。

3. **reasoning_content 分离**：`output` 是动作编号（控制逻辑只解析这个），`reasoning_content` 是 CoT 推理（帮助模型"想清楚再回答"）。类似 DeepSeek-R1 的推理模式，但动作输出只有一个 token，解析简单可靠。

---

## 四、对比实验设计及预期结果

### 4.1 实验设置

- **仿真环境**：Webots 3D，20m × 20m × 3m 室内场景
- **训练场景**：100 个（seed 0–99），medium 难度
- **测试场景**：20 个（seed 100–119），与训练集不重叠，含 easy/medium/hard
- **评测指标**：

| 指标 | 说明 |
|---|---|
| 成功率 (SR) | 到达目标的场景占比 |
| 碰撞率 (CR) | 碰撞障碍的场景占比 |
| 超时率 (TR) | 600 步未到达也未碰撞 |
| 平均步数 (AS) | 成功场景中的平均步数 |
| 路径效率 (PE) | 实际路径长度 / 起终直线距离 |

### 4.2 对比方法

| 方法 | 说明 |
|---|---|
| **随机策略** | 每步随机选 0–9 动作 |
| **直行策略** | 始终朝目标方向移动，不避障 |
| **3D 势场法** | 经典势场法，引力=目标，斥力=障碍 |
| **未微调 Qwen2.5-7B** | zero-shot，无训练 |
| **SOTA LLM (Claude)** | 数据收集时的专家本身，作为性能上限参考 |
| **微调后 Qwen2.5-7B (SFT)** | 本课题方案 |

### 4.3 消融实验

| 实验 | 变量 | 目的 |
|---|---|---|
| 有无 reasoning_content | 训练数据含/不含 CoT | CoT 是否提升决策质量 |
| 有无上下文累积 | 多轮对话 vs 每步独立 | 时序信息是否有用 |
| 数据量 | 30 / 60 / 80 条对话 | 数据量与性能关系 |
| 难度泛化 | 训练 medium，测试 easy/hard | 跨难度泛化能力 |

### 4.4 预期结果

**主实验**：

| 方法 | 预期 SR | 预期 CR | 预期 AS | 预期 PE |
|---|---|---|---|---|
| 随机 | ~5% | ~85% | ~500 | ~5.0 |
| 直行 | ~10% | ~80% | ~30 | ~1.0 |
| 势场法 | ~55% | ~30% | ~120 | ~1.7 |
| 未微调 7B | ~15% | ~60% | ~300 | ~3.0 |
| SOTA LLM (专家上限) | ~75% | ~15% | ~85 | ~1.4 |
| **微调后 7B (我们的)** | **~60%** | **~25%** | **~100** | **~1.6** |

**预期发现**：

1. **微调有效**：微调后 7B（~60% SR）显著高于未微调 7B（~15% SR），证明 SFT 数据有效。
2. **超越经典方法**：微调后 7B（~60%）超过势场法（~55%），因为 LLM 能理解"飞越矮障碍"而势场法只会绕行。
3. **不及 SOTA LLM**：微调后 7B（~60%）低于 SOTA LLM（~75%），蒸馏有损失。但 7B 推理快 10 倍以上、可本地部署。
4. **CoT 有帮助**：含 reasoning_content 的模型应比不含的 SR 高 5–10 个百分点。
5. **上下文累积有帮助**：多轮对话比每步独立 SR 高，但后期上下文变长推理变慢，存在效率权衡。

---

## 五、未来工作


1.**RL 微调（GRPO/PPO）**,以仿真奖励为信号，在 SFT 基础上继续优化，突破蒸馏天花板

2. **视觉输入**：当前用文本序列化状态，后续接入 Webots 摄像头渲染图，改用 VLM（如 Qwen2.5-VL）做视觉理解。从"读文本描述障碍物"升级为"看图像识别障碍物"。

3. **连续动作空间**：当前用 10 个离散动作，后续改为连续 `(vx, vy, vz, yawrate)` 输出。LLM 输出数值需要更精细的训练，但控制更平滑。

4. **Sim-to-Real**：当前仿真动作格式 `(vx, vy, vz, yawrate)` 与 `drone-navigation-main` 平台的 `motion_control_ws.py` 完全一致。后续可将微调模型部署到真机 Crazyflie，通过 OpenClaw 网关或 Hermes Agent 接入现有飞行控制链路。

5. **动态障碍物**：当前障碍物静态，后续加入移动障碍物（行人、其他无人机），LLM 需要预测障碍物运动轨迹。

6. **开放词汇指令**：从"到达目标点"升级为"飞到桌子旁边，看看上面有没有红色杯子"——结合 EQA（具身问答），让 LLM 同时做导航和问答。